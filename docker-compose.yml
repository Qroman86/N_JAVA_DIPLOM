version: '3.8'

services:
  cloud-app:
    build: .
    container_name: cloud-storage-app
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "5005:5005"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/cloud_db
      - SPRING_DATASOURCE_USERNAME=cloud
      - SPRING_DATASOURCE_PASSWORD=cloud
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect
      - JAVA_TOOL_OPTIONS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005
    volumes:
      - ./uploads:/uploads
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:15-alpine
    container_name: cloud-storage-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cloud_db
      POSTGRES_USER: cloud
      POSTGRES_PASSWORD: cloud
    ports:
      - "5432:5432"
    volumes:
      - ./build/docker/sql/01-schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./build/docker/sql/02-data.sql:/docker-entrypoint-initdb.d/02-data.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cloud -d cloud_db"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  uploads: